# Fallback values for lib/config.ts's four required variables (ARCHITECTURE.md
# §8), used ONLY by the Playwright/Vitest test path (lib/config.ts's
# applyTestEnvFallback) — never by `next build`/`next start`/`next dev`.
# Placeholder values only — never a real URL, key, or salt (SEC-7).

NEXT_PUBLIC_SUPABASE_URL=https://test-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=test-anon-key
SUPABASE_SERVICE_ROLE_KEY=test-service-role-key
SOURCE_REF_SALT=test-source-ref-salt
