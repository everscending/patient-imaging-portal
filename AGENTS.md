# Patient Imaging Portal

## Agent skills

### Issue tracker

Issues live in Linear, team `Jordan` (key `JOR`), project **Patient Imagine Portal**. See `docs/agents/issue-tracker.md`.

### Triage labels

Default triage vocabulary, except the two roles Loom owns — `ready-for-agent` and `wontfix` are carried by Linear's Status field, not by labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
